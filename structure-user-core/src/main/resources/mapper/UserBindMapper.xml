<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.structured.user.mapper.UserBindMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="cn.structured.user.entity.UserBind">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="platform_user_id" property="platformUserId"/>
        <result column="platform_code" property="platformCode"/>
        <result column="create_time" property="createTime"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id
        , user_id, platform_user_id, platform_code, create_time
    </sql>

</mapper>
